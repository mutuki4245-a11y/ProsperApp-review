# ProsperApp 要件定義書

作成日: 2026-07-03
対象: ProsperApp 店舗用アプリ
用途: 開発引継ぎ

## 1. 文書の位置づけ

本書は ProsperApp の業務要件を、現行実装と既定方針が分かる形で整理した開発引継ぎ用ドキュメントである。実装詳細は `Docs/システム仕様書.md`、作業方針やSQL/RPC参照情報の最新メモは `HANDOFF.md` も参照する。

状態区分は以下の通り。

| 状態 | 意味 |
| --- | --- |
| 実装済み | 現行コード、DB、または運用設定に実装が確認できる要件 |
| 一部実装 | アプリ側の処理はあるが、外部側の正式仕様、画面、または運用定義が残っている要件 |
| 運用方針 | 実装作業ではなく、現行運用や開発時に維持する前提 |
| 後続仕様 | 未実装で、別途仕様定義してから実装する要件 |
| 検討候補 | 効果や破棄契機を確認したうえで実装判断する改善案 |

## 2. システム目的

| 項目 | 状態 | 要件 |
| --- | --- | --- |
| 店舗業務の一元化 | 実装済み | 営業中の伝票管理、注文、会計、締め作業を店舗スタッフが同一アプリ上で処理できるようにする。 |
| 営業中操作の優先 | 実装済み | ルート `/` を営業中画面とし、当日伝票一覧、伝票起こし、勤怠管理導線を優先して表示する。 |
| 営業フロー事故防止 | 運用方針 | 締め作業と管理画面より先に、営業中トップ、伝票詳細、注文端末、会計完了までの誤卓、未保存、誤会計を防ぐ。見た目の刷新より、保存状態、対象伝票、会計内容を取り違えないことを優先する。 |
| 現場端末の役割分担 | 運用方針 | 現場運用は営業中画面を操作する端末1台と、注文入力用の `/Orders` 端末複数台を前提にする。営業中端末は伝票作成、当日伝票一覧、カラオケ簡易入力、会計導線、締め導線を扱い、注文端末は既存open伝票への注文登録に集中する。 |
| 締め作業の集約 | 実装済み | 酒代入力、勤怠確認、キャスト売上額調整、領収書入力、営業日締めを `/Closing` 配下に集約する。 |
| マスタ設定の分離 | 実装済み | キャスト情報、商品情報は営業フローの必須ステップではなく、上部タブの `マスタ設定` から開く任意管理領域とする。 |
| DBアクセスの統制 | 実装済み | アプリからのDB操作は Supabase RPC を `prosper-rpc` Edge Function経由で実行する。REST RPC fallbackと直接テーブルREST操作は持たない。 |

## 3. 利用者と権限

| 利用者 | 状態 | 要件 |
| --- | --- | --- |
| 店舗スタッフ | 実装済み | Googleログイン後、営業中画面、伝票、注文、締め作業を操作できる。 |
| 管理者 | 実装済み | `/Settings` で管理者設定を解除し、店舗選択、画面モード、管理者モードを端末に保存できる。勤怠刻みとキャスト売上額調整設定は店舗別マスターで管理する。 |
| 非管理者 | 実装済み | 商品管理ではカテゴリ編集操作を表示せず、カテゴリ保存POSTも拒否する。 |
| 許可アカウント | 実装済み | Googleログインは `GoogleAuth:AllowedEmails` または `GoogleAuth:AllowedDomains` に該当するメールアドレスだけを許可する。 |

## 4. 業務範囲

| 業務領域 | 状態 | 範囲 |
| --- | --- | --- |
| 店舗選択 | 実装済み | Supabaseの店舗マスタから利用店舗を選択し、端末ローカル設定として保存する。 |
| 営業日管理 | 実装済み | 現在営業日を取得し、営業入力時に営業日がなければ自動作成する。未締めの前営業日がある場合は新規営業日を作らず締め作業へ誘導する。 |
| 勤怠 | 実装済み | 出勤キャスト、出勤時刻、退勤時刻、送り利用有無を管理する。退勤時刻は締め前に必須確認する。 |
| 伝票管理 | 実装済み | 卓番、客、指名キャスト、注文、会計状態を伝票単位で管理する。 |
| 注文入力 | 実装済み | open伝票を選択し、カテゴリ別の商品ボタンから注文キューへ追加して一括登録する。 |
| 会計 | 実装済み | 伝票詳細から会計開始と現金会計確定を行う。 |
| 会計前確認 | 実装済み | 会計確定直前に、卓番、客数、退店時刻、合計、決済内訳、現金預かり、釣銭を同じ会計モーダル内で確認できるようにする。追加の確認ページや独立ステップは増やさない。 |
| 締め作業 | 実装済み | 未会計伝票、酒代、勤怠、キャスト売上額調整、領収書入力完了を確認して営業日を締める。 |
| 領収書簡易入力 | 実装済み | 未処理領収書を表示し、Google Driveプレビューを見ながら日付、金額、科目、摘要などを入力する。入力内容はDocManagement契約payloadとして組み立て、ProsperAppのRPCへ渡す。 |
| スキャンミス除外 | 実装済み | スキャンミスはDB上の論理削除として扱い、領収書入力対象から除外する。 |
| 勤怠入力改善 | 実装済み | キャスト追加はチェックボックスで複数名を一括登録でき、出勤時刻と退勤時刻は店舗営業時間に合わせた初期値と24:00以降表記で選択できる。 |
| 指名入力改善 | 実装済み | 指名種別は店舗別DBマスタの有効行から選択し、指名料金を明示的に選択できる。指名料金は毎回選択し、システム商品として注文行に自動追加して会計額へ加算する。 |
| 指名種別別キャストバック | 実装済み | 店舗別DBマスタで指名種別、表示名、同伴時刻、バック単価、有効/無効を管理し、指名登録時に有効な単価を営業実績へスナップショット保存する。 |
| 料金・バック用語 | 運用方針 | 会計額へ加算する料金は指名料金、指名時にキャストへ支払うバックは指名バック、商品注文時にキャストへ支払う通常バックはドリンクバック、商品注文バック対象が当該伝票の指名キャストだった場合のバックは担当バックと呼び分ける。 |
| 注文入力改善 | 実装済み | `/Orders` では違う卓のオーダーも同じキューへ追加して一括登録でき、バック対象商品のキャスト選択では「なし」を選べるようにする。注文端末には標準商品だけを表示し、システム商品だけのカテゴリは表示しない。 |
| 会計額調整 | 実装済み | 通常商品とは別枠の自由入力明細を用意し、名前と価格を入力して会計額の調整や値引きに使えるようにする。商品マスタには登録せず、価格は負値も許容し、当面は会計合計額へ直接加減する。 |
| カラオケ | 実装済み | カラオケは商品マスタ上のシステム商品として扱い、1回200円固定で通常の商品小計に含める。サービス料20%の対象とし、伝票/会計ではオーダー明細として表示する。 |
| 会計時領収書印刷 | 実装済み | 会計確定時の領収書印刷データ作成、SII Web SDK Serverへのブラウザ直接印刷、80mm紙向けの領収書レイアウト、印刷失敗時も会計確定を取り消さない処理、営業中トップの再印刷待ち導線を実装済み。 |

## 5. 機能要件

### 5.1 ログイン

| ID | 状態 | 要件 |
| --- | --- | --- |
| F-LOGIN-01 | 実装済み | `/Login` を除くRazor PageとDriveプレビューendpointは認証済みユーザーだけが利用できる。 |
| F-LOGIN-02 | 実装済み | Google OAuth設定が不足している場合、OAuthへ進まず `/Login` に設定不足を表示する。 |
| F-LOGIN-03 | 実装済み | Google認証失敗、許可外アカウント、リモート失敗は `/Login` にエラー表示する。 |
| F-LOGIN-04 | 実装済み | `/logout` でセッションとCookie認証を破棄する。 |

### 5.2 端末設定

| ID | 状態 | 要件 |
| --- | --- | --- |
| F-SET-01 | 実装済み | `/Settings` は固定パスワード `4245` で管理者設定を解除する。 |
| F-SET-02 | 実装済み | 店舗設定は `ProsperApp.LocalSettings` としてCookieとlocalStorageに保存する。 |
| F-SET-03 | 実装済み | サーバー処理ではCookieの `StoreDepartmentId` を優先し、ない場合は `Supabase:StoreDepartmentId` にフォールバックする。 |
| F-SET-04 | 実装済み | 勤怠時刻の選択肢は店舗別マスターの `attendance_minute_step` に従う。 |
| F-SET-05 | 実装済み | キャスト売上額調整では、売上基準と分配方式を店舗別マスターの `cast_sales_amount_basis` と `cast_sales_split_mode` から利用する。 |
| F-SET-06 | 実装済み | 端末設定の画面モードは、営業中端末向けの `sales-management` と注文端末向けの `order-entry` を持つ。`order-entry` 端末は営業中トップへアクセスしても `/Orders` へ誘導する。 |

### 5.3 営業中

| ID | 状態 | 要件 |
| --- | --- | --- |
| F-SALES-01 | 実装済み | 営業中画面は営業日、未会計数、会計済数のサマリーを表示する。 |
| F-SALES-02 | 実装済み | 営業中画面から卓番、来店時刻、客名、指名、メモを入力して伝票を作成できる。 |
| F-SALES-03 | 実装済み | 伝票作成時に営業日がなければ、店舗営業日の正午切替ルールに基づいて自動作成する。 |
| F-SALES-04 | 実装済み | 当日伝票一覧から伝票詳細へ遷移できる。 |
| F-SALES-05 | 実装済み | 営業中画面から勤怠管理へ遷移できる。 |
| F-SALES-06 | 実装済み | 営業中画面では卓ごとの会計額を常に隠し、画面右下寄りの固定オーバーレイボタンに触れている間だけ会計額を表示する。 |
| F-SALES-07 | 実装済み | 営業中画面ではカラオケを伝票行の `+` / `-` ボタンで簡易入力できる。数量変更は画面と会計額へ即時反映し、手動保存ボタンや短周期の自動保存は置かず、アプリ内遷移や他フォーム送信の前に未保存分を保存する。カラオケは商品マスタ上のシステム商品で、1回200円かつサービス料対象とする。 |
| F-SALES-08 | 運用方針 | 営業中画面は1台の営業中端末が中心的に操作する。営業中端末のカラオケ数量や会計額は操作直後にローカル表示へ即時反映し、保存通信はアプリ内遷移や他フォーム送信の前にまとめて実行する。複数注文端末からの注文はDB保存後の再取得または画面更新で反映する。 |
| F-SALES-09 | 実装済み | 営業中トップのカラオケ数量が未保存の場合、アプリ内リンク遷移や他フォーム送信の前に保存し、保存成功後だけ遷移または送信を続ける。ブラウザ再読み込みは運用上不要な操作として防止し、未保存時は警告対象にする。 |

### 5.4 伝票詳細

| ID | 状態 | 要件 |
| --- | --- | --- |
| F-SLIP-01 | 実装済み | 伝票詳細は客、指名、注文、会計の情報を同一画面で確認できる。 |
| F-SLIP-02 | 実装済み | 客を追加し、客名を更新し、退店時刻を登録できる。 |
| F-SLIP-03 | 実装済み | 指名キャストを追加できる。 |
| F-SLIP-04 | 実装済み | 注文を追加し、注文行を取消できる。 |
| F-SLIP-05 | 実装済み | 会計開始後、現金預かり額を入力して会計確定できる。 |
| F-SLIP-06 | 実装済み | 指名追加のキャスト候補は、退勤済みかどうかに関わらず、当日出勤キャスト全員を表示する。 |
| F-SLIP-07 | 実装済み | 指名種別は店舗別DBマスタの有効行から選択する。初期定義は本指名、場内指名、同伴「19:29まで」「19:59まで」「20:59まで」「21:00以降」の6種類とする。 |
| F-SLIP-08 | 実装済み | 指名種別の次に指名料金のUIを配置し、1000円から20000円まで1000円刻みで毎回選択できる。指名料金は `item_type = 'nomination_fee'` のシステム商品として注文行に自動追加し、会計額へ加算する。指名登録時は店舗別の指名バック設定を参照し、有効かつ0円より大きいバック単価をキャスト別の営業実績として保存する。 |
| F-SLIP-09 | 実装済み | 伝票には通常商品とは別枠で、名前と価格を自由入力できる調整明細を専用モーダルから追加できる。商品マスタには登録せず、価格は負値を許容し、伝票/会計では通常商品とは別の明細グループとして表示する。当面は会計合計額へ直接加減する。未保存入力は会計時ではなく、モーダルを閉じる時点で保存せず破棄するか確認する。 |
| F-SLIP-10 | 実装済み | カラオケは商品として伝票/会計のオーダー明細に表示し、数量と1回200円の固定単価から金額を計算する。時刻列は入店時刻とし、異なるタイミングで追加したカラオケも同一伝票内では1行に集約する。伝票詳細では指名料金と同じ自動システム商品として表示し、数量変更や保存操作は置かない。 |
| F-SLIP-11 | 実装済み | 会計取消では、確定済み会計と支払明細を取消して伝票を営業中へ戻す。客行の退店状態と退店時刻は変更せず、会計に紐づくキャスト売上額調整はリセットする。 |
| F-SLIP-12 | 実装済み | 会計モーダルでは、誤卓と誤会計を防ぐため、卓番、客名またはラベル、人数、利用時間、退店時刻、合計、支払内訳を確定ボタン周辺で確認できるようにする。 |
| F-SLIP-13 | 実装済み | 会計確定時は、会計端末が確認した保存済み伝票スナップショットとDB最新状態をRPC内で厳密比較する。伝票状態、客数、退店時刻、注文、自由入力明細、数量、単価、合計、決済内訳が不一致の場合は確定せず、会計モーダル内で再確認へ戻す。会計確定前に追加の事前RPCは増やさない。 |

### 5.5 注文入力

| ID | 状態 | 要件 |
| --- | --- | --- |
| F-ORDER-01 | 実装済み | `/Orders` でopen伝票を卓番として選択できる。 |
| F-ORDER-02 | 実装済み | 商品カテゴリ別の商品ボタンから注文キューへ追加できる。 |
| F-ORDER-03 | 実装済み | 同じ商品はキュー上で数量加算し、一括登録後は卓番選択へ戻る。 |
| F-ORDER-04 | 実装済み | `/Orders` では違う卓のオーダーも同じキューに入れて一括登録できる。キュー行は対象卓または対象伝票を保持する。 |
| F-ORDER-05 | 実装済み | バック対象商品のバックキャスト選択候補は、先頭に「なし」を表示する。「なし」を選んだ注文行にはバックの摘要を付与しない。 |
| F-ORDER-06 | 実装済み | バック対象商品のバックキャスト候補は、退勤済みかどうかに関わらず、当日出勤キャスト全員を表示する。 |
| F-ORDER-07 | 運用方針 | `/Orders` は複数の注文端末から同時利用する前提とする。注文キューや未送信ドラフトは端末ごとのローカル状態として保持し、登録成功後にDB/RPCを共有状態とする。注文端末間または営業中端末とのローカルドラフト共有は行わない。 |
| F-ORDER-08 | 実装済み | `/Orders` は営業中端末の上部タブや業務フローナビに含めない注文端末専用画面とする。マスタ設定と営業中への戻り導線は表示しないが、管理者設定への導線は画面右上に残す。画面は卓番選択、商品一覧、注文キューの3パネルを横並びにし、画面全体は縦横ともスクロールさせず、各パネル本文だけを縦スクロールできるようにする。 |
| F-ORDER-09 | 実装済み | `/Orders` の商品一覧と注文登録は標準商品だけを対象にする。カラオケなどのシステム商品は注文端末から登録できず、注文可能な商品がないカテゴリは表示しない。 |
| F-ORDER-10 | 実装済み | `/Orders` の注文端末キューは、DB保存前の端末内状態として扱う。会計端末や他の注文端末から参照できる未保存状態とはみなさず、会計前の未保存ブロック対象に含めない。 |
| F-ORDER-11 | 実装済み | 伝票詳細のオーダー追加モーダル内キューは、伝票詳細画面の保存前候補であり、`/Orders` の注文端末キューとは別物として扱う。画面全体や他端末に共有されるキューとして扱わない。 |
| F-ORDER-12 | 実装済み | 注文保存時は、RPC側で対象伝票が選択店舗に属し、会計済みではなくopen状態であることを確認する。無効な伝票への注文保存は失敗扱いとし、端末内キューを消さず再確認できるようにする。 |

### 5.6 マスタ設定

| ID | 状態 | 要件 |
| --- | --- | --- |
| F-MST-01 | 実装済み | `/Management` はキャスト情報、商品情報、指名バック設定への入口を表示する。 |
| F-MST-02 | 実装済み | キャスト設定は `/Management/Casts` に配置し、有効キャスト一覧、キャスト作成、キャスト削除を扱う。 |
| F-MST-03 | 実装済み | 商品設定は `/Management/Items` に配置し、商品カテゴリ、商品、価格、キャストバック設定を管理する。 |
| F-MST-04 | 実装済み | 商品名は店舗内uniqueとして扱う。 |
| F-MST-05 | 実装済み | 商品は追加、削除、並び替えで運用し、注文履歴は商品マスタを再参照しない。カラオケは `item_type = 'karaoke'` のシステム商品として扱い、通常の商品削除では削除できない。 |
| F-MST-06 | 実装済み | 指名バック設定は `/Management/NominationBacks` に配置し、店舗別DBマスタの指名種別ごとにバック単価と有効/無効を管理する。 |

### 5.7 締め作業

| ID | 状態 | 要件 |
| --- | --- | --- |
| F-CLOSE-01 | 実装済み | `/Closing` は酒代入力、勤怠確認、キャスト売上額調整、領収書入力を独立パネルで表示する。 |
| F-CLOSE-02 | 実装済み | 酒代は0円以上の整数として保存できる。酒代がない場合も0円で入力済みにする。 |
| F-CLOSE-03 | 実装済み | 勤怠確認では退勤時刻と送り利用有無を保存できる。 |
| F-CLOSE-04 | 実装済み | キャスト売上額調整は会計済みかつ指名キャストがいる伝票を対象に、専用ページとモーダルで保存する。 |
| F-CLOSE-05 | 実装済み | 領収書入力は営業日締めの必須条件とし、未入力領収書がある場合は営業日締めを実行できない。 |
| F-CLOSE-06 | 実装済み | 営業日締めは、未会計伝票0、酒代入力済み、勤怠1名以上、退勤未入力0、キャスト売上額調整済み、領収書入力完了を満たす場合だけ実行できる。 |
| F-CLOSE-07 | 実装済み | 領収書入力完了を含む営業日締め条件は、画面POSTと `store.close_business_day` RPCの両方で検証する。 |
| F-CLOSE-08 | 実装済み | 勤怠入力のキャスト追加モーダルでは、チェックボックスで複数キャストを選択して一括で出勤登録できる。 |
| F-CLOSE-09 | 実装済み | 勤怠時刻選択では、出勤時刻は19:00、退勤時刻は24:00をUI初期値にする。選択肢は各初期値の前後12時間を表示し、24:00以降は25:00、26:00のように表記する。 |
| F-CLOSE-10 | 実装済み | 管理者モードでは、営業日IDの一致確認は維持したまま、締め条件を画面POSTと `store.close_business_day` RPCの両方で無視して締められる。 |

### 5.8 領収書入力

| ID | 状態 | 要件 |
| --- | --- | --- |
| F-REC-01 | 実装済み | `/Closing/Receipts` は未処理領収書を1件ずつ表示し、次へ進める。 |
| F-REC-02 | 実装済み | Google Drive PDFはアプリサーバー経由の `/DrivePreview/{driveFileId}` で表示する。 |
| F-REC-03 | 実装済み | 領収書入力は `C:\Users\mutuk\Desktop\DocManagement\docs\field-app-contract.md` の契約に従い、証憑ID、会社、部門、仕訳、証憑関連をDBへ反映する。 |
| F-REC-04 | 実装済み | スキャンミスはDB上の論理削除として処理し、領収書入力対象から除外する。 |
| F-REC-05 | 運用方針 | `Sql/quick_entry_account_master_updates.sql` は領収書簡易入力で使う科目・補助科目の現在定義を確認する参照資料として扱う。 |
| F-REC-06 | 実装済み | 領収書入力内容は `save_journal_payload` 契約の `journal_entries`、`journal_entry_lines`、`document_journal_links` 形式でpayload化する。DocManagementアプリや `document-api` へ直接送信しない。 |

### 5.9 会計時領収書印刷

| ID | 状態 | 要件 |
| --- | --- | --- |
| F-PRINT-01 | 実装済み | 会計時にSII製レシートプリンター向けの領収書印刷要求データを作成できる。 |
| F-PRINT-02 | 実装済み | `ReceiptPrinter:Enabled = true` の場合、会計確定後に営業中トップへ戻り、同じブラウザで `siiWebSdk.js` の `PrinterManager` を使ってSII Web SDK Serverへ接続し印刷する。 |
| F-PRINT-03 | 実装済み | 印刷失敗、タイムアウト、SII Web SDK Server未起動、設定不足があっても会計確定は完了扱いとする。失敗した印刷要求は同じブラウザのlocalStorageへ再印刷待ちとして残し、営業中トップから再印刷または完了扱いにできる。 |
| F-PRINT-04 | 実装済み | 80mm幅の紙を前提に、店舗名、現在時刻、伝票番号、「飲食代として」、会計額、支払い方法、内消費税額を印字する。会計額が50,001円以上の場合だけ収入印紙欄を印字する。 |

## 6. 非機能要件

| 分類 | 状態 | 要件 |
| --- | --- | --- |
| セキュリティ | 実装済み | Cookie認証とGoogle OAuthを利用し、全Razor Pageを認証必須にする。 |
| DB権限 | 実装済み | RLSは有効化し、アプリ用操作は `security definer` RPCで制御する。アプリ側RPCはEdge Function経由に限定し、直接PostgREST RPC実行権限は付与しない。 |
| 秘密情報 | 運用方針 | Supabase RPCキー、Google認証情報、Azure publish profileはリポジトリへ保存しない。 |
| 店舗スコープ | 実装済み | 主要RPCのpayloadには `p_department_id` を含め、店舗単位で処理する。 |
| 可用性 | 運用方針 | 動作確認はAzure App Serviceへデプロイした環境で行う。ローカル起動を前提にしない。 |
| 操作性 | 実装済み | スマートフォン/PWA利用を想定したviewport、manifest、apple-touch-icon、処理中オーバーレイを備える。 |
| 現場操作レスポンス | 実装済み | `/Orders`、営業中カラオケ、自由入力明細は、画面内の操作結果へ即時反映する。営業中カラオケは手動保存や短周期の自動保存を置かず、アプリ内遷移や他フォーム送信の前に差分だけPOST/RPCで保存する。自由入力明細は専用モーダル内で入力と保存を完結させる。1タップごとに保存通信を発生させない。 |
| RPC回数抑制 | 運用方針 | 会計フローの快適性を保つため、会計確定前に追加の事前確認RPCを増やさない。厳密な一致確認は会計確定RPC内で行い、RPC回数は増やさずに事故防止を担保する。 |
| 操作ロック | 実装済み | 登録RPCや画面遷移待ちでは共通の処理中オーバーレイと送信ボタン無効化で追加操作を止める。個別画面では、共通ロックを通らない独自fetch、部分更新、特殊な遷移処理だけを追加確認対象にする。 |
| 端末間同期 | 運用方針 | 営業中画面1台と `/Orders` 複数台の運用では、端末間の共有状態はDB/RPC保存後のデータを基準にする。localStorageや画面内ドラフトは端末内だけで完結させ、他端末へ直接同期しない。 |
| 送信復旧 | 実装済み | 注文登録、勤怠保存、会計確定、カラオケ数量保存、自由入力明細保存は、送信中の二重送信を防ぐ。失敗時は入力値を消さず、自由入力明細はモーダル内で再送または保存せず破棄を選べるようにする。 |
| 画面初期データ | 実装済み | 卓、商品、出勤キャスト、営業日状態など操作中に使う候補は画面初期表示で取得し、候補選択や数量変更のたびに追加RPCを呼ばない。 |
| 保守性 | 実装済み | 画面はRazor Pages、業務DTOは `Features`、外部I/Oは `Infrastructure` と `Endpoints` に分離する。 |

## 7. 外部サービス要件

| サービス | 状態 | 要件 |
| --- | --- | --- |
| Supabase | 実装済み | 業務RPCは `prosper-rpc` Edge Function経由に限定する。アプリはEdge Functionキーだけを持ち、DB接続はEdge Function側の `SUPABASE_DB_URL` で行う。 |
| DocManagement | 実装済み | 領収書入力は `C:\Users\mutuk\Desktop\DocManagement\docs\field-app-contract.md` のpayload契約だけを参照する。直接API連携は持たない。 |
| Google OAuth | 実装済み | Googleログイン、メールアドレス許可、Driveアクセスtoken保存に利用する。 |
| Google Drive | 実装済み | 領収書PDFプレビューと先読みに利用する。 |
| SIIレシートプリンター | 一部実装 | 会計時の領収書印刷に利用する。端末側で接続済みのプリンターを対象とし、ブラウザからSII Web SDK Server経由で印刷する。 |
| SII Web SDK Server | 一部実装 | ブラウザアプリとSII製レシートプリンターの間に入る端末側アプリとして扱う。ブラウザは `siiWebSdk.js` の `PrinterManager` を使い、端末ローカルのSII Web SDK Serverへ接続する。 |
| Azure App Service | 運用方針 | 本番/確認環境として利用し、環境変数でSupabase/Google設定を注入する。 |
| GitHub Actions | 実装済み | `main` pushまたは手動実行でAzure App Serviceへデプロイするワークフローを持つ。 |

## 8. 運用制約

| 項目 | 状態 | 制約 |
| --- | --- | --- |
| SQL管理 | 運用方針 | SQLファイルは現在のDB内容を確認するための参照資料として扱う。DB反映はCodexがCLI等で実行する。 |
| Azure設定 | 実装済み | `Supabase__Url`、Edge Function URL/キー、Google OAuthを環境変数として設定する。Supabase API keyによる直接REST RPC経路は使わない。 |
| ローカル設定 | 実装済み | 店舗選択、画面モード、管理者モードなど端末固有設定はCookie/localStorageに保存される。勤怠刻み、売上額基準、売上額人数割は店舗別マスターで管理する。 |
| キー方式 | 実装済み | アプリからのRPCは `x-prosper-rpc-api-key`、`apikey`、Bearerに同じEdge Functionキーを設定して呼び出す。 |

## 9. 実装予定・方針のみの整理

### 9.1 後続仕様・検討候補

| 項目 | 状態 | 整理 |
| --- | --- | --- |
| レシートプリンター正式連携 | 一部実装 | 会計時の印刷要求データ作成、SII Web SDK Serverへのブラウザ直接印刷、80mm紙向けの指定項目レイアウト、失敗時に会計を取り消さない処理、営業中トップの再印刷待ち導線は実装済み。プリンター実機別の印字調整、SII Web SDK Serverの端末起動・接続設定は実機環境で確認する。 |
| 当日出勤キャスト候補キャッシュ | 実装済み | `store.get_order_attending_casts` は店舗別・営業日別に `IMemoryCache` へ初回成功時だけ保持する。勤怠保存、退勤情報保存、営業日開始、営業日締めの成功時に営業日単位キャッシュを破棄する。 |
| 営業中一覧と注文対象伝票の再取得削減 | 実装済み | `store.get_business_day_slips` と `store.get_order_entry_slips` はRazor初期表示をブロックせず、初回Ajax、フォーカス復帰、30秒ごとの表示中自動更新で取得する。保存成功POST直後の同期再取得は行わず、`/Orders` で候補から消えた伝票は選択と未送信キューから外す。 |

### 9.2 実装タスクではなく維持する運用方針

| 項目 | 整理 |
| --- | --- |
| 端末役割 | 営業中画面端末1台と `/Orders` 端末複数台を前提にし、端末間のローカルドラフト同期は行わない。共有状態はDB/RPC保存後のデータを基準にする。 |
| 秘密情報 | Supabase RPCキー、Google認証情報、Azure publish profileはリポジトリへ保存しない。 |
| 確認環境 | 動作確認はAzure App Serviceへデプロイした環境を前提にする。 |
| SQL管理 | SQLファイルは現在定義の参照資料として維持し、DB反映はCodexがSupabase CLIまたはSupabaseコネクタで実行する。 |
